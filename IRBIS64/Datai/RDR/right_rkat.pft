/* g1 - содержит маску КАТЕГОРИИ ЧИТАТЕЛЯ
/* g2-g10 ЗАНЯТЫ
/* формат возвращает 1 если g1 соответствует одному из повторений v50
if (v50='') and (g1='') then '1' else 
       (if v50<>'' then if &uf('=!',&uf('Ag1#1'),'!',v50)='1' then '1' break fi fi),
fi
